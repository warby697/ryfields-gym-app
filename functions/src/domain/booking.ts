export function chooseBookingPlacement(bookedCount:number,capacity:number,waitlistCount:number){if(!Number.isInteger(bookedCount)||!Number.isInteger(capacity)||!Number.isInteger(waitlistCount)||bookedCount<0||capacity<1||waitlistCount<0)throw new Error('Invalid capacity values');const waitlisted=bookedCount>=capacity;return{status:waitlisted?'waitlisted' as const:'confirmed' as const,position:waitlisted?waitlistCount+1:null}}
