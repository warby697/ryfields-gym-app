import { applicationDefault,initializeApp } from 'firebase-admin/app'
import { FieldValue,getFirestore,Timestamp } from 'firebase-admin/firestore'
initializeApp({credential:applicationDefault()});const db=getFirestore(),now=new Date(),day=86400000
const plans=[['full',{name:'Full Gym',description:'Gym floor and all group classes',priceMinor:4500,currency:'GBP',billingInterval:'monthly',active:true,classAccessPolicy:'all'}],['classes',{name:'Classes',description:'Group classes membership',priceMinor:3500,currency:'GBP',billingInterval:'monthly',active:true,classAccessPolicy:'classes_only'}]]
for(const[id,data]of plans)await db.collection('membershipTypes').doc(id).set({...data,createdAt:FieldValue.serverTimestamp(),updatedAt:FieldValue.serverTimestamp()},{merge:true})
await db.collection('locations').doc('main').set({name:'Ryfields Gym',timezone:'Europe/London',active:true},{merge:true})
for(const[index,item]of[['Strength Circuit',6,30,45,20,'Chris'],['Yoga Flow',9,30,60,16,'Maya'],['BoxFit',18,0,60,20,'Dan']].entries()){const[name,hour,minute,duration,capacity,instructor]=item,start=new Date(now.getTime()+day);start.setHours(Number(hour),Number(minute),0,0);await db.collection('classSessions').doc(`staging-${index}`).set({nameSnapshot:name,startsAt:Timestamp.fromDate(start),endsAt:Timestamp.fromMillis(start.getTime()+Number(duration)*60000),locationSnapshot:`Studio ${index===2?2:1}`,instructorNames:[instructor],capacity,bookedCount:0,waitlistCount:0,status:'scheduled',bookingOpensAt:Timestamp.fromMillis(start.getTime()-14*day),bookingClosesAt:Timestamp.fromDate(start),cancellationCutoffAt:Timestamp.fromMillis(start.getTime()-120*60000),createdAt:FieldValue.serverTimestamp(),updatedAt:FieldValue.serverTimestamp()},{merge:true})}
console.log('Staging reference data seeded. No members or payments were created.')
